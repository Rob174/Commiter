from setuptools import setup

setup(
  name = 'commiter',
  packages = ['commiter'], # this must be the same as the name above
  version = '0.1',
  description = 'Check that all code is correctly committed',
  author = 'Robin M',
  author_email = 'robin.128.456@gmail.com',
  url = 'https://github.com/Rob174/Commiter',
  download_url = 'https://github.com/Rob174/Commiter/0.1',
  keywords = ['commit', 'ai' "git"], 
  classifiers = [],
  install_requires=[],
)